apiVersion: v1
kind: Service
metadata:
  name: {{ include "referenceLettersSystem.fullname" . }}
  labels:
    {{- include "referenceLettersSystem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "referenceLettersSystem.selectorLabels" . | nindent 4 }}
