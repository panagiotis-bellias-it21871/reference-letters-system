apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "referenceLettersSystem.fullname" . }}-test-connection"
  labels:
    {{- include "referenceLettersSystem.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "referenceLettersSystem.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
